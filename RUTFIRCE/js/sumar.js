//----------------------------------------------------------
//---------------------------------------------------------
function sumar() {
    const primeraprenda = prenda(prompt("Ingrese la primera  prenda que desee"));
    const segundaprenda = prenda(prompt("Ingrese la  segunda prenda que desee"));
}
